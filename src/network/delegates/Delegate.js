class Delegate {
  constructor (core) {
    this.core = core
  }

  /**
   * Handle given packet blocks.
   * @param {Object} parameters Packet block data
   */
  handle () {
    // ...
  }
}

export default Delegate
